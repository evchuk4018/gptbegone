# API Client Module

Owns browser-to-backend HTTP and stream protocol behavior.

## Responsibilities

- REST wrappers for chats, settings, and model management.
- SSE stream parsing for assistant response chunks.

## Public Entry Point

- `api` object in `client.ts`
